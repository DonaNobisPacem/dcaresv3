<%= simple_form_for(@component_attachment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :attachment %>
    <%= f.association :component %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
