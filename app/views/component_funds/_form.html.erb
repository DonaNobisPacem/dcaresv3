<%= simple_form_for(@component_fund) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :source %>
    <%= f.input :amount %>
    <%= f.association :component %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
